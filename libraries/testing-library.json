{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": "Groups Testing Library packages",
  "packageRules": [
    {
      "description": "@testing-library/react peer-depends on a @testing-library/dom version range - splitting a non-major bump of one from a still-gated major bump of another installs mismatched versions. Same failure mode as libraries/graphiql.json.",
      "matchPackagePatterns": ["^@testing-library/"],
      "groupName": "all testing-library packages",
      "separateMajorMinor": false
    }
  ]
}
