{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": "Groups Storybook development environment",
  "packageRules": [
    {
      "description": "@storybook/* addons require an exact major-version match with the storybook core package - splitting a non-major bump of one from a still-gated major bump of another installs mismatched versions. Same failure mode as libraries/graphiql.json.",
      "matchPackageNames": ["storybook"],
      "matchPackagePatterns": ["^@storybook/"],
      "groupName": "all storybook packages",
      "separateMajorMinor": false
    }
  ]
}
